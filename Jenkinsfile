pipeline {
    agent { label 'jenkinagent01' }
    stages {
        stage('build') {
            steps {
                sh 'echo building something'
            }
        }
        stage('config') {
            steps {
                sh 'echo config something'
            }
        }
    }
}
